interface:
  display_name: "Word Diet"
  short_description: "Puts the agent's replies on a diet."
  default_prompt: "Use Word Diet: answer like a cornerman between rounds, zero filler, only what changes the next round, while keeping commands, code, file paths, and error strings exact. Check live sources before calling facts that may have changed."
